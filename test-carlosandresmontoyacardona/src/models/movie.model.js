export default class MovieModel {

    constructor (title, release, description, image) {
        this.title = title;
        this.release = release;
        this.description = description;
        this.image = image;
    }
}